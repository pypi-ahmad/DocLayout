[pytest]
testpaths=tests
markers =
    filename(name): specify the filename for the pdf_document fixture
    integration: billable end-to-end GPT-6 Sol checks; requires --run-integration
    cpu: runs offline without API calls
    config(values): per-test configuration
    output_format(name): requested output format
filterwarnings =
    ignore::Warning

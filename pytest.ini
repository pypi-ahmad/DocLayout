[pytest]
testpaths=tests
markers =
    integration: billable end-to-end GPT-6 Sol checks; requires --run-integration
    cpu: runs offline without API calls
    config(values): per-test configuration
filterwarnings =
    ignore::Warning
